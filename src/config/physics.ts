/** Physics constants for the planet (Tech Doc §6). */

/** Radial gravitational acceleration magnitude pulling bodies toward the planet center. */
export const PLANET_GRAVITY = 18;

/** How long (seconds) a flammable body burns before it is removed. */
export const BURN_DURATION = 6;
